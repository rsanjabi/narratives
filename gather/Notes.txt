Notes on possible graph format:


Types:

category:
    tags:
        additional_tags - common (can be filtered on) or not
        freeform (??)
        (relationships: parent_tags (more general - media), synonym_tags, sub_tags, child_tags (characters, relationships, additional_tags)
        relationships:
        characters
        fandoms

works
bookmarks
comments

users:
    psueds